import{useEffect,useRef}from'react';import{gsap,scopedAnimation}from'../animations/scrollAnimations';const steps=['MAKE IT OBVIOUS','MAKE IT EASY','MAKE IT ATTRACTIVE','MAKE IT REPEATABLE'];export default function DesigningHabit(){const ref=useRef(null);useEffect(()=>scopedAnimation(ref,()=>{const q=gsap.utils.selector(ref);gsap.timeline({scrollTrigger:{trigger:ref.current,start:'top top',end:'+=220%',pin:true,scrub:1}}).from(q('.design-step'),{autoAlpha:0,xPercent:60,stagger:.45}).from(q('.specific'),{autoAlpha:0,scale:.9},1.4)}),[]);return <section ref={ref} className="panel design dark"><h2>DESIGNING A HABIT</h2><div className="steps">{steps.map(s=><div className="design-step" key={s}>{s}<span>↓</span></div>)}</div><div className="specific"><del>I will study more.</del><strong>After dinner, I will study for 20 minutes at my desk.</strong></div></section>}
