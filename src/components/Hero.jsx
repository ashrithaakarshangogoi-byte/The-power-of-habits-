import { useEffect, useRef } from 'react';
import { gsap, scopedAnimation } from '../animations/scrollAnimations';
export default function Hero(){const ref=useRef(null);useEffect(()=>scopedAnimation(ref,()=>{const q=gsap.utils.selector(ref);gsap.timeline({scrollTrigger:{trigger:ref.current,start:'top top',end:'+=140%',pin:true,scrub:1}}).to(q('.hero-title span'),{y:-90,letterSpacing:'0.08em',stagger:.03},0).to(ref.current,{backgroundColor:'#111318',color:'#f7f4ed'},0).to(q('.orbit'),{autoAlpha:1,scale:1,rotate:360},.15).to(q('.scroll-cue'),{autoAlpha:0,y:30},0);}),[]);return <section ref={ref} className="panel hero"><div className="kicker">Behavioral science / visual essay</div><h1 className="hero-title" aria-label="The Power of Habits">{'THE POWER OF HABITS'.split('').map((l,i)=><span key={i}>{l===' '?'\u00A0':l}</span>)}</h1><p>Small actions. Repeated consistently. Big changes.</p><div className="orbit" aria-hidden="true"><i/><i/><i/></div><div className="scroll-cue">SCROLL TO EXPLORE</div></section>}
