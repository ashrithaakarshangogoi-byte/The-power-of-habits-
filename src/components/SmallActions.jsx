import{useEffect,useRef}from'react';import{gsap,scopedAnimation}from'../animations/scrollAnimations';export default function SmallActions(){const ref=useRef(null);useEffect(()=>scopedAnimation(ref,()=>{const q=gsap.utils.selector(ref);gsap.timeline({scrollTrigger:{trigger:ref.current,start:'top top',end:'+=170%',pin:true,scrub:1}}).fromTo(q('.growth'),{scale:.25},{scale:5}).from(q('.small-copy'),{autoAlpha:0,y:40},.35)}),[]);return <section ref={ref} className="panel small-actions"><div className="growth">1%</div><p className="small-copy">Small improvements can become significant when repeated over time.</p></section>}
