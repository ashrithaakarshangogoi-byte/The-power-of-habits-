import{useEffect,useRef}from'react';import{gsap,scopedAnimation}from'../animations/scrollAnimations';export default function EnvironmentSection(){const ref=useRef(null);useEffect(()=>scopedAnimation(ref,()=>{const q=gsap.utils.selector(ref);gsap.timeline({scrollTrigger:{trigger:ref.current,start:'top top',end:'+=190%',pin:true,scrub:1}}).to(q('.phone'),{x:260,y:90,autoAlpha:.25,rotate:20}).to(q('.book'),{x:-120,y:-40,scale:1.25},0).to(q('.noise'),{autoAlpha:0,y:80,stagger:.1},0).from(q('.tool'),{autoAlpha:0,y:-50,stagger:.1},.25)}),[]);return <section ref={ref} className="panel environment"><h2>CHANGE THE ENVIRONMENT.</h2><p>Willpower is not the only factor. The environment around you can make behaviors easier or harder to repeat.</p><div className="desk" aria-label="Animated desk showing distractions moving away"><i className="obj phone">phone</i><i className="obj book">book</i><i className="obj noise">tabs</i><i className="obj noise">snacks</i><i className="obj tool">notes</i><i className="obj tool">lamp</i></div></section>}
